apiVersion: v1
kind: Pod
metadata: 
  name: nginx
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
      # resources:
      #   requests:
      #     memory: "200mi"
      #     cpu: "250m"
      #   limits:
      #     memory: "250mi"
      #     cpu: "500m"
